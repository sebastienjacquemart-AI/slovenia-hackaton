[project]
name = "forecasting-data-pipeline"
version = "0.1.0"
description = "Polars pipeline for the forecasting challenge dataset"
requires-python = ">=3.11,<3.14"
dependencies = [
    "autogluon-tabular[lightgbm]>=1.5,<2",
    "lightgbm>=4.6,<5",
    "numpy>=2.0,<3",
    "pandas>=2.2,<4",
    "polars>=1.20,<2",
]

[tool.uv]
package = false
